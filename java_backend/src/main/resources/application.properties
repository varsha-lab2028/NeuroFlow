server.port=8080
spring.application.name=neuroflow-backend

# Allow cross-origin from Flask frontend on port 5001
app.cors.allowed-origins=http://localhost:5001

# SQLite DB path (relative to where the JAR is run from)
app.db.path=neuroflow.db
